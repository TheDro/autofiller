import _ from 'lodash'
console.log('hello world!')


// // watch keyboard inputs into any textarea
// document.addEventListener('input', event => {
//     // get content of the textarea and current cursor position
//     const content = event.target.value
//     const cursor = event.target.selectionStart
//     console.log({content, cursor})

//     // add grayed out text"this is the way" to the text area
//     const grayedOut = 'this is the way'
//     const grayedOutLength = grayedOut.length

//     const before = content.slice(0, cursor)
//     const after = content.slice(cursor)

//     event.target.value = before + grayedOut + after
    

// })